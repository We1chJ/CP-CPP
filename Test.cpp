#include "bits/stdc++.h"

using namespace std;
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    set<pair<int, int>> s;
    s.insert({1, 89});
    s.insert({1, 35235});
    cout << s.size() << endl;
}