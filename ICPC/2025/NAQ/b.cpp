#include "bits/stdc++.h"

using namespace std;

int main(){

    cin.tie()->sync_with_stdio(0);

    int c, r , n;
    cin >> c >> r >> n;
    vector<vector<int>> ans1 (r, vector<int>(c, -1));
    vector<vector<int>> ans2 (r, vector<int>(c, -1));

    

}